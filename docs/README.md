# aether — техническая документация

Статус: начальная реализация, A05, A06, A08 и A09 выполнены; A07 выполнена частично. Основной стек: Python 3.12, uv, PyTorch. Установлен каркас CLI; модель, сервер и обучение пока не реализованы. Доступны `uv run --locked aether --help` и `--version`; `train --config configs/model/tiny.json --validate-only` проверяет строгую схему без backend. `inspect --config ... --manifest ...` проверяет синтетический bundle. Обучение закрыто, остальные рабочие команды явно завершаются как нереализованные.

Начинать работу с [единого плана задач](tasks.md): там находятся полный реестр, выполненные пункты, зависимости и критерии приёмки. Текущая машина используется только для разработки и ограниченных тестов. Всё обучение, включая короткие обучающие проверки, выполняется в удалённом runtime платного Google Colab через notebook; local runtime не используется.

## Как читать

| Документ | Содержание |
|---|---|
| [tasks.md](tasks.md) | Единый план, статусы, пошаговые задачи и работа через Colab |
| [specification.md](specification.md) | Требования, границы, обязательные решения и критерии результата |
| [architecture.md](architecture.md) | Общая архитектура и объяснение компонентов |
| [model.md](model.md) | Математика, конфигурация модели, тензоры, кодек и временное выравнивание |
| [runtime.md](runtime.md) | Порядок выполнения, состояния, протокол, буферы и ошибки |
| [training.md](training.md) | Данные, обучение, функции потерь и сохранение экспериментов |
| [development.md](development.md) | Структура Python-пакета, uv, зависимости и рабочий процесс |
| [validation.md](validation.md) | Проверки реализации, измерения и этапы приёмки |
| [data.md](data.md) | Краткая идентификация проекта |

## Статусы требований

- **Обязательно** — условие реализации и приёмки.
- **Начальная конфигурация** — выбранная отправная точка; изменение требует версии конфигурации и повторной оценки.
- **Эксперимент** — гипотеза, которую нельзя выдавать за готовую возможность.
- **Открытое решение** — недостающий выбор, необходимый перед соответствующим этапом.

При расхождении обзорного текста и подробного контракта применяются: `tasks.md` для статусов и распределения работ между локальной машиной и Colab, `specification.md` для области проекта, `model.md` для вычислений, `runtime.md` для транспорта, `development.md` для инструментов, `validation.md` для приёмки. Расхождение исправляется до реализации затронутого компонента.

## Текущая готовность

Документирован путь от проверки тензоров и аудиокодека до живого full-duplex разговора и контролируемого дообучения. Выбраны английский язык, демо чат-бота, Google Drive и бюджет первой стадии 500 юнитов. Пока не выбраны комплект совместимых весов, целевая GPU и данные. Поэтому наличие спецификации не означает готовность обучить систему с известным бюджетом или запустить голосовую модель одной командой.

Команды модели в документах — целевой интерфейс будущего приложения. `pyproject.toml`, группы зависимостей и `uv.lock` уже созданы. Проверки каркаса: `uv run --locked pytest`, `uv run --locked ruff check .`, `uv run --locked ruff format --check .`, `uv run --locked mypy src/aether`.

Схемы и ограничения описаны в [configuration.md](configuration.md). Выравнивание токенов реализовано без модельного backend; это не проверка качества речи. Для первого результата выбран начальный кодек, затем планируется кодек Manifestro с отдельной проверкой совместимости.

Локально реализованы [проверка артефактов](artifacts.md) и [аудиобуферы](audio.md); реальный state_dict, токенизатор и качество речи ещё не проверены.

Готов локально проверенный [notebook preflight](../notebooks/aether_colab.ipynb) и [инструкция Colab](colab.md). A16/A17 остаются открытыми до удалённой проверки.
